{% extends 'project/project-template.html.twig' %}

{% block project_body %}
    <table class="table table-hover table-bordered">
        <thead>
        <tr>
            <th scope="col">Nom</th>
            <th scope="col">Project Owner</th>
            <th scope="col">Addresse</th>
            <th scope="col">Ville</th>
            <th scope="col">Architecte</th>
            <th scope="col">Entreprise Générale</th>
            <th scope="col">Création</th>
            <th scope="col">Modifier</th>
            <th scope="col">Supprimer</th>
        </tr>
        </thead>
        <tbody>
        {% for project in projects %}
            <tr>
                <th><a href="#">{{ project.name }}</a></th>
                <th>{{ project.projectOwner.name }}</th>
                <th>{{ project.address }}</th>
                <th>{{ project.city }}</th>
                <th>{{ project.architect.name }}</th>
                <th>{{ project.generalCompany.name }}</th>
                <th>{{ project.created | date('d/m/Y') }}</th>
                <th class="text-center"><a href="#"><i class="fas fa-pen"></i></a></th>
                <th class="text-center"><a href="#"><i class="fas fa-trash-alt"></i></a></th>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}